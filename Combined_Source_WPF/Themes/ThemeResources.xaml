<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========================================== -->
    <!-- VS Code Dark Palette (HEX Colors)          -->
    <!-- ========================================== -->
    <Color x:Key="VSCode.Colors.Editor.Background">#0A0A0A</Color>
    <Color x:Key="VSCode.Colors.Editor.Foreground">#D4D4D4</Color>
    <Color x:Key="VSCode.Colors.Sidebar.Background">#141414</Color>
    <Color x:Key="VSCode.Colors.Sidebar.Border">#2A2A2A</Color>
    
    <Color x:Key="VSCode.Colors.Accent.Blue">#047857</Color>
    <Color x:Key="VSCode.Colors.Accent.BlueHover">#059669</Color>
    <Color x:Key="VSCode.Colors.Accent.BluePressed">#065F46</Color>

    <!-- Controls Config -->
    <Color x:Key="VSCode.Colors.Control.Background">#1E1E1E</Color>
    <Color x:Key="VSCode.Colors.Control.Border">#333333</Color>
    <Color x:Key="VSCode.Colors.Control.HoverBorder">#0D6EFD</Color>
    <Color x:Key="VSCode.Colors.Control.InputBackground">#141414</Color>
    
    <!-- Secondary Colors -->
    <Color x:Key="VSCode.Colors.Button.SecondaryBackground">#383838</Color>
    <Color x:Key="VSCode.Colors.Button.SecondaryHover">#4A4A4A</Color>
    
    <!-- Text Colors -->
    <Color x:Key="VSCode.Colors.Text.Muted">#9E9E9E</Color>
    <Color x:Key="VSCode.Colors.Text.Disabled">#606060</Color>

    <!-- ========================================== -->
    <!-- SolidColorBrushes                          -->
    <!-- ========================================== -->
    <SolidColorBrush x:Key="VSCode.Brushes.Editor.Background" Color="{StaticResource VSCode.Colors.Editor.Background}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Editor.Foreground" Color="{StaticResource VSCode.Colors.Editor.Foreground}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Sidebar.Background" Color="{StaticResource VSCode.Colors.Sidebar.Background}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Sidebar.Border" Color="{StaticResource VSCode.Colors.Sidebar.Border}" />
    
    <SolidColorBrush x:Key="VSCode.Brushes.Accent.Blue" Color="{StaticResource VSCode.Colors.Accent.Blue}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Accent.BlueHover" Color="{StaticResource VSCode.Colors.Accent.BlueHover}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Accent.BluePressed" Color="{StaticResource VSCode.Colors.Accent.BluePressed}" />

    <SolidColorBrush x:Key="VSCode.Brushes.Control.Background" Color="{StaticResource VSCode.Colors.Control.Background}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Control.Border" Color="{StaticResource VSCode.Colors.Control.Border}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Control.HoverBorder" Color="{StaticResource VSCode.Colors.Control.HoverBorder}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Control.InputBackground" Color="{StaticResource VSCode.Colors.Control.InputBackground}" />

    <SolidColorBrush x:Key="VSCode.Brushes.Button.SecondaryBackground" Color="{StaticResource VSCode.Colors.Button.SecondaryBackground}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Button.SecondaryHover" Color="{StaticResource VSCode.Colors.Button.SecondaryHover}" />
    
    <SolidColorBrush x:Key="VSCode.Brushes.Text.Muted" Color="{StaticResource VSCode.Colors.Text.Muted}" />
    <SolidColorBrush x:Key="VSCode.Brushes.Text.Disabled" Color="{StaticResource VSCode.Colors.Text.Disabled}" />

    <!-- ========================================== -->
    <!-- WPF Control Styles                         -->
    <!-- ========================================== -->

    <!-- Primary Accent Button Style -->
    <Style x:Key="VSCode.Styles.Button.Accent" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource VSCode.Brushes.Accent.Blue}" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="BorderBrush" Value="#3c3c3c" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center" 
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource VSCode.Brushes.Accent.BlueHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource VSCode.Brushes.Accent.BluePressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="#252526" />
                            <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Text.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Secondary Gray Button Style -->
    <Style x:Key="VSCode.Styles.Button.Secondary" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource VSCode.Brushes.Button.SecondaryBackground}" />
        <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Editor.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Control.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center" 
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource VSCode.Brushes.Button.SecondaryHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#2A2C2E" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="#222222" />
                            <Setter Property="BorderBrush" Value="#2B2B2B" />
                            <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Text.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Flat TextBox Style -->
    <Style x:Key="VSCode.Styles.TextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource VSCode.Brushes.Control.InputBackground}" />
        <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Editor.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Control.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="5,3" />
        <Setter Property="CaretBrush" Value="{StaticResource VSCode.Brushes.Editor.Foreground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost" 
                                      Focusable="False" 
                                      HorizontalScrollBarVisibility="Hidden" 
                                      VerticalScrollBarVisibility="Hidden"
                                      Margin="2" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="#858585" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Control.HoverBorder}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="#222222" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="#2D2D2D" />
                            <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Text.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Custom CheckBox Style -->
    <Style x:Key="VSCode.Styles.CheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Editor.Foreground}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <Border x:Name="chkBorder" 
                                Width="16" 
                                Height="16"
                                Background="{StaticResource VSCode.Brushes.Control.InputBackground}" 
                                BorderBrush="{StaticResource VSCode.Brushes.Control.Border}" 
                                BorderThickness="1" 
                                CornerRadius="2"
                                SnapsToDevicePixels="True"
                                VerticalAlignment="Center">
                            <Path x:Name="chkCheckMark" 
                                  Width="10" 
                                  Height="10" 
                                  SnapsToDevicePixels="True"
                                  Stretch="Uniform" 
                                  Fill="#FFFFFF" 
                                  Data="M 3 6 L 5 8 L 9 2 M 3 6 L 5 8 L 9 2"
                                  Stroke="#FFFFFF" 
                                  StrokeThickness="1.5" 
                                  Visibility="Collapsed" />
                        </Border>
                        
                        <ContentPresenter Grid.Column="1" 
                                          Margin="8,0,0,0" 
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" 
                                          SnapsToDevicePixels="True"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="chkCheckMark" Property="Visibility" Value="Visible" />
                            <Setter TargetName="chkBorder" Property="Background" Value="{StaticResource VSCode.Brushes.Accent.Blue}" />
                            <Setter TargetName="chkBorder" Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Accent.Blue}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="chkBorder" Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Control.HoverBorder}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="chkBorder" Property="Background" Value="#222222" />
                            <Setter TargetName="chkBorder" Property="BorderBrush" Value="#2B2B2B" />
                            <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Text.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Custom ToggleButton Style (for FileSelectionWindow) -->
    <Style x:Key="VSCode.Styles.ToggleButton" TargetType="ToggleButton">
        <Setter Property="Background" Value="{StaticResource VSCode.Brushes.Button.SecondaryBackground}" />
        <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Editor.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Control.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center" 
                                          Margin="{TemplateBinding Padding}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource VSCode.Brushes.Button.SecondaryHover}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource VSCode.Brushes.Accent.Blue}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Accent.Blue}" />
                            <Setter Property="Foreground" Value="#FFFFFF" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="#222222" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="#2B2B2B" />
                            <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Text.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Flat ComboBox Style (for Presets Selection) -->
    <Style x:Key="VSCode.Styles.ComboBox" TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource VSCode.Brushes.Control.InputBackground}" />
        <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Editor.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Control.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,5" />
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Editor.Foreground}" />
                    <Setter Property="Padding" Value="10,8" />
                    <Setter Property="SnapsToDevicePixels" Value="true" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Bd"
                                        Padding="{TemplateBinding Padding}"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0"
                                        SnapsToDevicePixels="true">
                                    <ContentPresenter HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="true">
                                        <Setter TargetName="Bd" Property="Background" Value="#2A2A2A" />
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="false">
                                        <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Text.Disabled}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid x:Name="templateRoot" SnapsToDevicePixels="true">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" />
                        </Grid.ColumnDefinitions>
                        <Popup x:Name="PART_Popup"
                               Grid.ColumnSpan="2"
                               AllowsTransparency="true"
                               IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                               Placement="Bottom"
                               PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                            <Border x:Name="dropDownBorder"
                                    MinWidth="{Binding ActualWidth, ElementName=templateRoot}"
                                    Margin="0,2,0,0"
                                    Background="{StaticResource VSCode.Brushes.Sidebar.Background}"
                                    BorderBrush="{StaticResource VSCode.Brushes.Sidebar.Border}"
                                    BorderThickness="1"
                                    CornerRadius="4">
                                <ScrollViewer x:Name="DropDownScrollViewer">
                                    <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                        <Canvas x:Name="canvas"
                                                Width="0"
                                                Height="0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Top">
                                            <Rectangle x:Name="opaqueRect"
                                                       Width="{Binding ActualWidth, ElementName=dropDownBorder}"
                                                       Height="{Binding ActualHeight, ElementName=dropDownBorder}"
                                                       Fill="{Binding Background, ElementName=dropDownBorder}" />
                                        </Canvas>
                                        <ItemsPresenter x:Name="ItemsPresenter"
                                                        KeyboardNavigation.DirectionalNavigation="Contained"
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                    </Grid>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                        
                        <!-- ToggleButton (Draws Background and Arrow) -->
                        <ToggleButton x:Name="toggleButton"
                                     Grid.ColumnSpan="2"
                                     Background="{TemplateBinding Background}"
                                     BorderBrush="{TemplateBinding BorderBrush}"
                                     BorderThickness="{TemplateBinding BorderThickness}"
                                     Foreground="{TemplateBinding Foreground}"
                                     IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                     Style="{x:Null}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="templateRoot"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4"
                                            SnapsToDevicePixels="true">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Path x:Name="arrow"
                                                  Grid.Column="1"
                                                  Margin="0,0,10,0"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                                  Fill="{TemplateBinding Foreground}" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Control.HoverBorder}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <!-- Non-Editable Content Area -->
                        <ContentPresenter x:Name="contentPresenter"
                                          Grid.Column="0"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource TemplatedParent}}"
                                          ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                          ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource TemplatedParent}}"
                                          IsHitTestVisible="false"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                        <!-- Editable Text Area -->
                        <TextBox x:Name="PART_EditableTextBox"
                                 Grid.Column="0"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Margin="{TemplateBinding Padding}"
                                 Focusable="true"
                                 IsReadOnly="{TemplateBinding IsReadOnly}"
                                 Visibility="Collapsed"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{TemplateBinding Foreground}"
                                 CaretBrush="{TemplateBinding Foreground}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="true">
                            <Setter Property="IsTabStop" Value="false" />
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            <Setter TargetName="contentPresenter" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="toggleButton" Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Control.HoverBorder}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="toggleButton" Property="Opacity" Value="0.56" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Flat ListBox & Item Style (for file selection list) -->
    <Style x:Key="VSCode.Styles.ListBox" TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource VSCode.Brushes.Control.InputBackground}" />
        <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Editor.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource VSCode.Brushes.Control.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource VSCode.Brushes.Editor.Foreground}" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#2A2D2E" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#04395E" />
                            <Setter Property="Foreground" Value="#FFFFFF" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
